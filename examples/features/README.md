# Feature Examples

These files mirror the examples in `docs/features/*.mdx`.

Verify them with:

```bash
pnpm --filter @secure-exec/example-features check-types
pnpm --filter @secure-exec/example-features test
```
